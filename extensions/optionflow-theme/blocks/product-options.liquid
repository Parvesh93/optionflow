<div
  class="optionflow-product-options"
  data-optionflow-root
  data-optionflow-handle="{{ block.settings.option_set_handle | escape }}"
  data-optionflow-show-title="{{ block.settings.show_title }}"
>
  <div class="optionflow-product-options__loading">
    {{ block.settings.loading_text | escape }}
  </div>
</div>

{% schema %}
{
  "name": "OptionFlow options",
  "target": "section",
  "javascript": "optionflow.js",
  "stylesheet": "optionflow.css",
  "settings": [
    {
      "type": "text",
      "id": "option_set_handle",
      "label": "Option set handle",
      "info": "Copy the handle from the OptionFlow option set editor."
    },
    {
      "type": "checkbox",
      "id": "show_title",
      "label": "Show option set title",
      "default": false
    },
    {
      "type": "text",
      "id": "loading_text",
      "label": "Loading text",
      "default": "Loading options..."
    }
  ]
}
{% endschema %}
